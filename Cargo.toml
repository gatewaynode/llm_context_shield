[package]
name = "llm_context_shield"
version = "0.4.0"
edition = "2024"
license = "MIT"
description = "Rust library and CLI for scanning LLM context injection threats"

[lib]
name = "llm_context_shield"
path = "src/lib.rs"

[[bin]]
name = "lcs"
path = "src/main.rs"
required-features = ["cli"]

[dependencies]
clap = { version = "4", features = ["derive"], optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt"], optional = true }
tracing-appender = { version = "0.2", optional = true }
toml = "0.8"
yara-x = { version = "1.14", optional = true }

[features]
default = ["cli"]
cli = ["dep:clap", "dep:tracing-subscriber", "dep:tracing-appender"]
yara = ["dep:yara-x"]

[lints.rust.unexpected_cfgs]
level = "warn"
check-cfg = ['cfg(feature, values("syara"))']

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
